[project]
name = "deployment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "usgs-triplifier",
]

[dependency-groups]
dev = [
    "ruff>=0.15.1",
]

[tool.uv.sources]
usgs-triplifier = { path = "../usgs-triplifier", editable = true }
